// const solution = (myString, pat) =>
myString.slice(0, myString.lastIndexOf(pat) + pat.length);
//   myString.substring(0, myString.lastIndexOf(pat)) + pat;

console.log(solution("AbCdEFG", "dE"));
